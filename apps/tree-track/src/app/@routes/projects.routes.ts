import { Route } from '@angular/router';
import { ProjectsComponent } from '../projects/feature/projects/projects.component';

export const projectsRoutes: Route[] = [
  {
    path: '',
    component: ProjectsComponent,
  },
];
